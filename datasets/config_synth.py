DEFAULT_TORCH_DTYPE = "float64"

# Number of samples
N_SAMPLES = 100
# Number of evaluation per function
N_THETA = 100
# Number of test samples
N_TEST = 100

# Seeds for drawing the GP dictionary
SEED_GPS = 65

# GPs parameters
STDS_GPS_IN = [0.05, 0.1, 0.5, 0.7]
STDS_GPS_OUT = [0.05, 0.1, 0.5, 0.7]
GP_SCALE = 1.5

